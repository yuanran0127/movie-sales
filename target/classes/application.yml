
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rabbit_movie?useSSL=true&useEncoding=true&characterEncoding=utf8&serverTimeZone=GMT
    username: root
    password: root
  thymeleaf:
    prefix: classpath:/static/pages/
    cache: false
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
